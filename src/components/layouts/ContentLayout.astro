<main class="flex flex-col w-full max-w-4xl min-h-screen border-l border-r border-gray-800">
  <header class="p-8 w-full flex flex-col text-white sticky top-0 bg-black z-10 border-b border-gray-800">
    <slot name="title"/>
  </header>
  <article class="prose prose-invert max-w-none p-8">
    <slot name="body"/>
  </article>
</main>